name: mini_collection_poc
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  blurhash_dart: ^1.2.1
  flutter:
    sdk: flutter
  flutter_blurhash: ^0.7.0
  flutter_shaders: ^0.1.1
  native_device_orientation: ^1.1.4
  tilt: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  golden_toolkit: ^0.15.0

flutter:
  assets:
    - assets/flaticon/
    - assets/unsplash/
  shaders:
    - shaders/blurhash43.glsl
  uses-material-design: true
